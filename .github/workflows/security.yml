name: Security

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    # Lunes 06:00 UTC: detecta CVEs nuevos aunque no haya commits.
    - cron: "0 6 * * 1"

permissions:
  contents: read

jobs:
  # Repo PÚBLICO: cualquier credencial commiteada queda expuesta (incidente 2026-07-21).
  # Escanea el diff completo del push/PR en busca de secretos.
  secretos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Dependencias de producción con vulnerabilidades conocidas (alta/crítica rompen el job).
  # TODO(fase-2): quitar continue-on-error al mergear Next 15 — hasta entonces
  # npm audit reporta avisos de Next 14 (EOL) que solo se corrigen en 15.5.x.
  dependencias:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm audit --omit=dev --audit-level=high
